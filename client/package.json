{
  "dependencies": {
    "@inquirer/prompts": "^7.1.0",
    "bcrypt": "^5.1.1",
    "fetch": "^1.1.0",
    "inquirer": "^12.1.0",
    "node-fetch": "^3.3.2",
    "typescript": "^5.6.3"
  },
  "name": "client",
  "version": "1.0.0",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "app": "mkdir dist && cd dist && npx tsc && node app.js",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/inquirer": "^9.0.7",
    "rimraf": "^6.0.1"
  }
}
